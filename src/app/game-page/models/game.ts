export interface Game {
  points: number;
  distance: number;
  location: string;
  longitude_guess: number;
  latitude_guess: number;
  longitude_real: number;
  latitude_real: number;
}
// created_at, latitude_guess, latitude_real, location, longitude_guess, longitude_real, points, updated_at, user_id
